<UserControl x:Class="AutoLCPR.UI.WPF.Views.ImportarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F8F9FA">
    <ScrollViewer>
        <StackPanel Margin="30" VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- Header -->
            <Border Background="White" BorderBrush="#E8EAED" BorderThickness="0,0,0,1" Padding="20,15" Margin="0,0,0,40" Width="600">
                <TextBlock Text="Importar" 
                           FontSize="24" 
                           FontWeight="SemiBold" 
                           Foreground="#202124"
                           TextAlignment="Center"/>
            </Border>

            <!-- Conteúdo -->
            <Border Background="White" 
                    BorderBrush="#E8EAED" 
                    BorderThickness="1"
                    Padding="60"
                    CornerRadius="8"
                    Width="600">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⬇️" 
                               FontSize="60" 
                               TextAlignment="Center"
                               Margin="0,0,0,20"/>
                    
                    <TextBlock Text="Implementação Futura" 
                               FontSize="28" 
                               FontWeight="SemiBold" 
                               Foreground="#202124"
                               TextAlignment="Center"
                               Margin="0,0,0,15"/>
                    
                    <TextBlock Text="Esta funcionalidade está em desenvolvimento e será disponibilizada em breve." 
                               FontSize="14" 
                               Foreground="#5F6368"
                               TextAlignment="Center"
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
